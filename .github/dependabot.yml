version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    ignore:
      # Pinned on purpose; update by hand after checking the API still matches.
      - dependency-name: events.vex
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
